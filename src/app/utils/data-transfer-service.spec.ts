import { DataTransferService } from './data-transfer-service';

describe('DataTransferService', () => {
  it('should create an instance', () => {
    expect(new DataTransferService()).toBeTruthy();
  });
});
